{
  "name": "continuum-mockup",
  "version": "0.0.0",
  "private": true,
  "description": "Mockup de l'app CONTINUUM et ses bancs. La géométrie (run_test.sh) tourne en node nu ; le banc de fidélité (tests/run_fidelite.sh) exige puppeteer.",
  "scripts": {
    "test": "sh run_test.sh",
    "fidelite": "sh tests/run_fidelite.sh"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0"
  }
}
